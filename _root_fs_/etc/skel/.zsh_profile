# Privacy
umask 077

# TMUX
export TMUX_M=principale
export TMUX_D=développement
ifprincipale [[ -z "$TMUX" ]] ;then
	(nohup tmux has-session -t $TMUX_M&>/dev/null&)||(sleep 4);(nohup tmux new-session -d -s $TMUX_M&>/dev/null&)
	(nohup tmux has-session -t $TMUX_D&>/dev/null&)||(sleep 4);(nohup tmux new-session -d -s $TMUX_D&>/dev/null&)
fi

# X at login
if [ -z "$DISPLAY" ] && [ -n "$XDG_VTNR" ] && [ "$XDG_VTNR" -eq 1 ]; then
	exec startx
fi

